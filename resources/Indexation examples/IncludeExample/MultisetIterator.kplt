#define bits=20

type Term {
  max {
    a_$i$ -> a_$i$ (Add) . :0<=i<=bits
  }
}

type Add {
    2a_$i$ -> a_$i+1$ . :0<=i<=bits
}

#include "MultisetIterator.kplt"

termA {@a_$i$:0<=i<=bits@, a, b, @c_$j$:1<=j<=9@} (Term) .

termB {@a_$i$:0<=i<=bits@} (Term) .

add {} (Add) .

termA - add .
termB - add .
term_$i$ - term_$i+1$ . : 0<=i<=2
